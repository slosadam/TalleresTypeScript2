export class Serie{

    constructor(public id: number, public nombre: string, public plataforma: string, public temporada: number,
        public descripcion: string, public dir: string, public foto: string ){
            
        }
    }
    